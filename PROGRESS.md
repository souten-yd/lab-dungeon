# 唐揚げダンジョン — 進捗ログ

> 締切版（セッション中断時の復元用）。最終更新: 2026-09-17 (JST) — **T50〜T55 全完了（T54=ワープゲートを「解放済みゲート階↔地上」の自由往来に / T55=階段ダイアログの階数ラベルの文字列結合バグ修正「11F/41F/101F」→計算後に結合・全回帰17スイート415/0 green）**

## 全体ステータス
| タスク | 状態 |
|---|---|
| T0〜T42 | ✅ 完了・検証済み |
| T37 BGM 31曲 | ✅ 生成・検証完了（テーマ30曲60.2〜68.2s/ボス84.5s→**T47で122.9sに再生成**・英語歌詞ボーカル・シームレスループ） |
| T38 SEファイル(20本) | ✅ 全20本完成・QA済（2秒窓方針） |
| T40 必殺TTS / T41 中ボスボイス | ✅ **廃止**（ユーザー指示09-15: 日本語TTSに不自然な部分→**ボイスなしで完成**） |
| T35 統合テスト+記録 | ✅ 完了（最終132/132 green + audio最終sweep + 記録更新） |
| **T43 中ボス専用BGM9曲** | ✅ **完了・検証済み（音源9/9インストール・QA合格・AUD_LISTとディスク完全一致）** |
| **T44 モバイルBGM自動再生** | ✅ 完了・検証済み（t44_test 9/9） |
| **T45 性能最適化** | ✅ 完了・計測済み（t45_perf_test 13/13・before/after計測下記） |
| **T46 モバイル・描画バグ修正** | ✅ 完了・検証済み（t46_test 29/29→T47でWeb Audio仕様に33/33） |
| **T47 Web Audio SFX+回転ズレ+BGM発音** | ✅ **全完了**（t47_test 14/14・全15スイートgreen / BGM 10曲`Ka-ra-a-ge`表記・約2分+ループで再生成・最終QA 10/10合格） |
| **T48 4点一括（BGM30再生成+回転ズレ根本修正+町2行目+上り階段）** | ✅ **全完了**（t48_test 36/36→T49で56/56 / BGM 30曲実尺332〜338sで30/30インストール・QA合格） |
| **T49 降り階段の整合性+選択制降下** | ✅ **全完了**（t48_test 56/56・全16スイート**338/0 green** / 上り階段の到着=降り階段の隣・踏むと「降りる/留まる」選択・従来分岐(100F/中ボス封鎖)維持） |
| **T50 斜め攻撃+ぶつかった音+スティック方向転換化** | ✅ **全完了**（t50_test 48/48・全17スイート**386/0 green** / 斜め攻撃は壁挟み(直交片方でも不可)で攻撃不可=ぶつかった音+向きのみ・移動の衝突にも音(0.25sスロットル)・左上スティック=ターン消費なしの方向転換・直線壁攻撃は従来どおりターン消費） |
| **T51 移動時のスプライト「一瞬縮む」修正** | ✅ **全完了→T52で改訂**（当初=実行時bbox計測`measureImgBBox`・ユーザー環境でサイレント失敗し再発したためT52で静的テーブル化へ） |
| **T52 T51再発根絶（実行時計測廃止+静的bboxテーブル）** | ✅ **全完了**（t50_test E1a〜E5 / `SPRITE_BB`=5職×静止/歩行10件をPILオフライン実測(α>8)でコード埋め込み・`measureImgBBox`+`getImageData`依存を完全削除=どの環境でも静止/歩行の表示サイズ完全一致） |
| **T53 上下移動で体の向き(左右)がリセットされない** | ✅ **全完了**（t50_test F1〜F4 / 体の向き`faceX`と狙い方向`dirIdx`の分離=上下移動で最後に移動した左右方向を維持・攻撃・ハイライト・8方向攻撃はdirIdxのまま不変） |
| **T54 ワープゲート=解放済みゲート階↔地上の自由往来** | ✅ **全完了**（t50_test G1〜G7 / ゲートは各10F帯の中ボスが守り・討伐で解放(=`SAVE.miniBossDefeated`永続)→「解放済みゲート階+地上(町)」間の自由往来に・未討伐は封鎖トーストのみ・miniGate再配置廃止・100Fゲート廃止(セーブポイント維持)・全回帰17スイート**408/0 green**） |
| **T55 階段ダイアログの階数ラベル=計算後に結合** | ✅ **全完了**（t50_test H1〜H7 / ボタン`"降りる ("+s.from+1+"F)"`=左から文字列結合で1F→「11F」/4F→「41F」/10F→「101F」→`stairsDescendText`/`stairsSubText`ヘルパーで`(from+1)`を先に計算・全回帰17スイート**415/0 green**） |

## 最新（2026-09-15 午後）

### T43 中ボス専用BGM — 完了（9/9）
- 設計: 10F〜90F帯の中ボス9体各自の「唐揚げ王国崇拝」BGM。全曲にヴォーカルフック**「Karaage!」**、各約2分。深層ほど狂信化: 10Fゴンタ=素朴な民間賛美歌 → 50F=聖戦の行進 → 90Fベリアル=終末の集団合唱。歌詞仕様 `tools/lyrics_t43_mb.md`（"The Fried Kingdom"モチーフ・T37「Karaage Emperor」と一貫）。
- 配線: `mbBgmName(floor)`/`isMbBgm(n)`・AUD_LIST 69→**78**（BGM 35→**44**）・`mbTalk`挑発時=その階専用曲(`bgm_mb_01〜09`)に切替・`mbDefeated`=フロースレームBGM復元。検証: t43_test 12/12 + t41_t42 D4 / t41_gatekeys K2 をT43仕様に更新。
- 音源(**9/9インストール・QA合格**): `bgm_mb_01`=118.2s / `bgm_mb_02`=119.2s / `bgm_mb_03`=123.5s / `bgm_mb_04`=121.7s / `bgm_mb_05`=118.5s / `bgm_mb_06`=120.9s / `bgm_mb_07`=117.3s / `bgm_mb_08`=121.9s / `bgm_mb_09`=120.6s（全曲110〜135s枠・peak -0.8〜-2.3dB・Vorbis 150k/48kHz stereo・クリップ0%）。`bash /tmp/opencode/qa_mb_bgm.sh`=**9 OK / 0 FAIL**。
- **GPU VRAM競合の経過**: 初回生成時、他プロセス(ユーザーの`llama-server` Qwen3.8-27B)がGPU[0]の34.2GB中~23GBを占有→ACE-Step LM初期化で「HIP out of memory」で4曲失敗。対策=`/tmp/opencode/retry_mb_bgm_blind.py`(**VRAMゲートなし・失敗ごとに即時再挑戦**のバックグラウンドループ)を起動→GPUが空いた窓(16:15/16:52-17:01)を自動捕捉して残4曲を全生成。検証済みコピーは`/tmp/opencode/mb_bgm_final/`(9本)。
- 最終確認済み: audio/=**78本**でAUD_LIST(78・BGM44+SFX34)と完全一致・`run_all_tests.sh`全13スイートgreen・`node --check`合格。

### T44 モバイル・タイトルBGM自動再生
- 報告: 「モバイルだとトップ画面で音楽がならない」。原因=ブラウザ自動再生ポリシー(初回ユーザー操作なしに`Audio.play()`不可)。
- 実装: `tryPlayBgm()`+`bgmPending`フラグ(BGM再生失敗/未開始でpending保持)+`unlockAudio()`=window `pointerdown`/`touchstart`と**ゲーム本体の既存keydownハンドラ内**に配線(第二window listenerは禁止=ハーネスが最後1件のみ保持するため)。初回操作でタイトルBGM開始+タイトル画面に「タップでサウンドON」ヒント表示。
- 検証: t44_test **9/9**（pending保持/タップで開始/二重再生防止/キー操作開始/ダンジョン中無効化）。

### T45 性能最適化（実装+計測完了）
報告: 「挙動が重い。攻撃時やアイテム取得時に特に固まる。また、敵が多い場合にも固まる。よく使うSEはキャッシュすることや、(…)動作表示を軽量化することで高速化できないか？最大数の敵が発生時にも処理落ちせずスムーズに処理して欲しい」。

**実施した3点:**
1. **床レイヤオフスクリーンキャッシュ** — 全マップ1792×1280(2240タイル)を`FLOOR_CV`に1回レンダリングし、毎フレームは可視領域の単一`drawImage`ブリット。静的部分(床/草/石・毒沼ベース・穴・柱・ロックドア・水+水際・2.5D壁影グラデ・隠し壁強調)を焼き込み、動的部分(毒泡・水波紋・pitScar・秘宝室グロー)は`drawFloorDynamics`が可視範囲のみ毎フレーム描画。`invalidateFloor()`フック: `enterFloor`/IMG onload/プレイヤー穴埋め(`stepOnHazard`)/敵穴埋め(`enemyStepHazard`)/秘宝室開錠/トラップ室ロック。トラップ(尖刺/転移)・開いた罠扉・スイッチパルス・tint・チャージ警告・エンティティ/パーティクル/floatは毎フレームのままで不変。
2. **`ctx.filter`全廃** — 攻撃/被弾・アイテム取得の3箇所のfilterヒットフラッシュを軽量オーバーレイ矩形に置換(白`rgba(255,255,255,.75)`/敵白/プレイヤー暗`rgba(20,30,60,.5)`)。filterはGPU合成コストが大きく、攻撃時スパイクの主因。
3. **SFXプール起動時予備生成** — 34種×5本=170 Audio要素を起動時に一括生成(共有decoded buffer)。初回再生時のAudio要素生成スパイクを解消(常時5本/種のプール維持)。

**before/after計測**（ヘッドレスハーネス・40敵・300フレーム・canvas APIカウント。before=編集前タイルループの忠実再構築+計測、after=現行コード実測）:

| メトリクス | 変更前 | 変更後 | 変化 |
|---|---|---|---|
| canvas呼び出し/フレーム | 2,090 | 1,163 | **−44%** |
| `fillRect`/フレーム | 1,094 | 69 | **−94%** |
| `createLinearGradient`/フレーム | 21 | 0 | **−100%**（壁影をキャッシュ化） |
| タイル`drawImage`/フレーム（実ブラウザ換算） | ~340 | **1**（ブリット） | **−99.7%** |
| JS時間/フレーム（ヘッドレス） | 0.173 ms | 0.100 ms | −42% |
| 攻撃/取得時の`ctx.filter`設定 | 各回~11フレーム | 0 | **全廃** |
| 敵48体・フレーム平均（t45 D1） | — | <8 ms（60fpsの1/13以下） | 60fps維持 |
| SFX Audio要素生成（初回再生時） | その都度生成 | 起動時一括170本（以後0生成） | スパイク解消 |

> 注: ヘッドレスではcanvas呼び出しはno-opのため実GPUコストは含まれない。実ブラウザではfillRect/drawImageの減少がGPUラスタ化+GPU→CPU合成の削減に直結し、攻撃時/取得時のスパイク(filter)は完全に消える。

**検証:** t45_perf_test **13/13**（A: プール生成方式+連打6回で5本維持 / B: FLOOR_CV存在+動的リスト+120fでdrawImage<3000・fillRect<20000・gradient 0 / C: 攻撃+10f・取得+5fでfilter設定0 / D: 敵48体で平均<8ms・<16.7ms・600f無例外）+ **全回帰green**+`node --check`合格。

### T46 モバイル・描画バグ修正（2026-09-15・報告3件+附帯1件）
**ユーザー報告(3件):** ①「移動していると動かなくなる」（モバイル・常に/頻繁に）②「敵を攻撃すると敵の背景が白くなる」③「横向きにしても画面に広がらない」。

**診断:** ヘッドレスハーネス(rAF駆動+phase/gate/画面プロブ)で**ゲームロジック健全を証明**（100フロールームランダムウォーク0例外・0実停滞・30回右長押し0デッドロック・9/9中ボス戦OK。観測された「phase=enemy停滞」は全てgameover画面の正当フリーズ）→ ①は環境レベル(モバイルChromeのAudio要素上限)と判定。

**実施した4修正:**
1. **SFXプール遅延化（①主因）** — 起動時一括生成(34種×5=170本)を**廃止**。起動Audio248本がChrome「Media resources were blocked for too long」の閾を超過し、モバイルで全メディアを恒久ブロック→BGM死+固まり。`playSFX(n)`が初回再生時にその種5本のみ生成(同一URLはdecoded buffer共有=メモリ増は不変)。起動Audio=AUD_LISTの78本のみ。
2. **ターンwatchdog（①保険）** — `TURN.phase`がanim/enemyで2秒以上復帰しない(overlay/ポーズ/画面変更なし)場合、`player`へ強制復帰+console.warnで停滞フェーズ記録。正常はanim≤0.16s+enemy≤0.08sで解決するため誤爆なし。
3. **被弾フラッシュ=シルエット（②）** — T45の白色矩形オーバーレイは**スプライトの透過四隅まで白塗り**が原因。初回フラッシュ時にオフスクリーンで`globalCompositeOperation="source-in"`白色塗り→`Image._white`(_dark)にキャッシュ、毎フレームコスト=drawImage 1回のみ(旧rectと同等・filterより軽量)。敵=白/プレイヤー=暗/ミミック=白。
4. **画面比に応じた横方向拡張（③）** — `const W=960`→`let W`（H=540固定）。`fitCanvas()`が端末比で`W=960〜min(1280,round(540×iw/ih))`に再計算(16:9以下=960維持のレターボックス)し`cv.width`+CSSで画面いっぱい表示。`resize`+`orientationchange`追従。全面背景(`bg_title/town/clear`)は`imgCover`(アスペクト保持coverクロップ)で引き伸ばし歪み防止。町ショップx=比率`fx`+`layoutTown()`毎フレーム追従、タッチUI(ABTN/PADMENU)=右端オフセット`dx`+`layoutTouchUI()`追従。カメラ表示域`viewW=W/ZOOM`が横に自動拡大。フロアキャッシュはマップ全体サイズ(W非依存)のため影響なし。
   - （附帯）P/Esc keydownで`e.repeat`を無視（OSキーリピートによるポーズ高速トグルの解消）。

**検証:** t46_test **29/29**（A: 起動Audio=78のみ+起動時プール無生成 / B: 初回playSFXで5本・連打で頭打ち・種独立 / C: フラッシュ中白矩形fillRect 0回+source-in使用+drawImage差2(通常1)+Imageキャッシュ / D: anim停滞>2s→player復帰(120f)・rAF生存・正常anim誤爆なし / E: 20:9でW=1201+cv.width/style追従・縦長は960維持・上限1280・町/タッチUI追従 / F: 連続移動12/12+watchdog誤爆なし / G: 拡張Wで180f+町/タイトル実動作スモーク）+ **全回帰14スイートgreen**(TOTAL **182** pass / 0 fail・計上外77を含む実259チェック)+`node --check`合格。t45_perf_test A1は「起動時無生成(遅延化)」仕様に更新済み。

### T47 モバイル2バグ+ボスBGM発音修正（2026-09-15・報告3件）
**ユーザー報告(3件):** ①「（T46後も）モバイルで固まる」②「縦から横に回転するとタッチボタンが全部ズレる」③「ボスBGMの『Karaage!』が『カラージ』に聞こえる」。

**① SFX=Web Audioへ全面移行:** T46の`<audio>`遅延プール(5本/種)でもモバイルChromeでフリーズ残存→**SFXの`<audio>`要素を全廃**。単一`AudioContext`+初回ユーザー操作(`unlockAudio()`・既存keydown/pointerdown/touchstart配線)で`resume()`+全34SFXのfetch+`decodeAudioData`を非同期実行→`SFX_BUF{name:AudioBuffer}`。`playSFX(n,rate)`=`createBufferSource()`+`playbackRate.value=rate||1`+`connect(destination)`+`start()`=**プール不要・同時重なり無制限**(ターン制連打に最適)。起動Audio生成=**BGM44のみ**(旧78→44)。未登録名(未来の追加SFX)は従来どおり`<audio>`フォールバック。`sfxWarmup`は`fetch`未定義環境(headless)でno-op。

**② 縦→横回転でタッチUIが全部ズレる:** 根因=回転遷移中`innerWidth/innerHeight`が**旧(縦長)値のまま返る**ブラウザの挙動で、`orientationchange`の150ms遅延実行や遅れてきた`fitCanvas`が**拡大済みのWを縮小値に確定**させ、描画(動的W)とタッチヒット判定(旧rect)が固定=ズレが解消しなかった。修正4点:
1. `orientationchange`の150ms`setTimeout`**廃止**→`fitCanvasNow()`=即`fitCanvas()`+rAF×2+`setTimeout(fitCanvas,60)`を**resizeとorientationchange両方**に绑定(遷移の全段階で再収束)。
2. **単調拡大保証**(`fitCanvas`内): 直近のW拡大(`W_T`)から600ms以内の縮小はスキップ(旧値の遅着を無視)。正当な縮小は600ms後に自動適用。
3. **`canvasPos()`アスペクトガード**: `getBoundingClientRect`の縦横比がバッファ(W/H)と2%以上食い違う=遷移中の旧rect→contain正規化でW/H空間に再写像(画面端タップがゲーム画面の端に一致)。
4. **`touchTol()`**: 回転直後(`FIT_T`)300msだけABTN/PADMENU/PAD8/JOYのヒット半径×1.2(収束前のタップも拾う)。

**③ ボスBGM「カラージ」問題:** `Karaage!`は英語歌唱で末尾/g/が/ji:/にglideし「カラージ」に。ユーザー指示で**まずprobe 1曲**(`audio/karaage_probe_01.ogg`・`Ka-ra-a-ge!`=モーラ区切り+伸ばしa+硬い`!`、2番に旧表記`Karaage!`/`Karaage~`をA/Bコントロールとして内蔵)を生成→**ユーザーが「唐揚げって聞こえるからよし」と確認**。**決定: 全10曲(9中ボス+大魔王)を`Ka-ra-a-ge`表記で再生成**(`tools/lyrics_t43_mb.md`/`lyrics_t37_boss.md`の歌詞・promptも全置換済み)。ユーザー追加指定: **大魔王`bgm_boss.ogg`=実尺≈2分(125spec)+シームレスループ**(旧84.5sは短すぎ)。QAゲート=実尺110〜135s/peak<-0.01dBFS/Vorbis 48kHz stereo+**ループシーム判定**(先頭0.5s RMSと末尾0.5s RMSの比が0.15〜6=末尾フェード→先頭ダウンビートの継ぎ目を検出)。blind retry(12回/曲・6h上限・VRAM窓捕捉)+watchdog(失敗曲の自動ピンポイント再実行)で実施。

**再生成結果(10/10完了・09-16 01:33):** blind retry+watchdogでllama-serverのVRAM競合を回避しつつ全曲生成。mb_05のみ1ラウンド12回失敗→watchdogが自動でピンポイント再実行し初回成功。最終QA **10/10合格**: `bgm_mb_01`=117.2s / `bgm_mb_02`=117.1s / `bgm_mb_03`=117.9s / `bgm_mb_04`=120.4s / `bgm_mb_05`=119.8s / `bgm_mb_06`=119.1s / `bgm_mb_07`=117.7s / `bgm_mb_08`=119.1s / `bgm_mb_09`=119.2s / `bgm_boss`=**122.9s**(旧84.5s→2分化)（全曲110〜135s枠・peak -0.8〜-4.2dB・Vorbis 48kHz stereo・ループシーム比0.70〜3.67=先頭/末尾のエネルギー継続を確認）。**ASR発音QA**: 新mb_01のフック行が「Karachi」と書き起こし=`Ka-ra-a-ge`の4モーラ構造が歌唱されている機械的確認（probeのユーザー確認と一致）。probe曲は`/tmp/opencode/karaage_probe_01.ogg`へ退避（audio/=78本=AUD_LISTと一致を維持）。

**検証:** t47_test **14/14**(A: 縦長起動W=960→resize即実行でW=1169/旧縦長値が600ms窓内で来てもW縮小なし/窓超過で正当縮小W=960/旧rectでcanvasPos右端→x≈W・中央→W/2・一致rectは従来スケーリング/touchTol 0-200ms=1.2→350msで1.0 / B: 起動Audio=44(BGMのみ)/SFX34種`<audio>`0/unlockでdecode34/playSFX=bufferSourceのみ・Audio不生成) + t46_test **33/33**(A/B=Web Audio仕様に書き換え・E/F=FakeDateで600ms窓クリア) + **全15スイートgreen**(TOTAL **201** pass/0 fail)+`node --check`合格。

### T48 4点一括（2026-09-16・ユーザー指示4件）
**ユーザー指示:** ①ダンジョンBGMを全30曲再生成（5分以上・10F帯エモーションアーク）②「縦画面から横画面に回転すると、見えているボタンの**1つ下**をタップしないと反応しない」がT47後も残存 ③町ショップ2行目が雑貨屋(5施設目)追加で重複/画面外溢出 ④**上り階段**を追加（「階段は下に降りた場所に上に行く階段を設置すること。また、上に行く階段は街に直通ではなく、一階上に戻るだけとすること」=**方案A**で確定）。

**① ダンジョンBGM 30曲再生成（30/30完了・09-16 21:50）:** `bgm_dun_00〜09_a/b/c`全30曲を**10F帯エモーションアーク**で再生成: 1-30F=冒険初心者・意気揚々 / 31-50F=冒険の辛さ / 51-70F=仲間喪失をバネに希望 / 71-90F=大財宝目前の希望 / 91-100F=唐揚げ帝国に踏み入れ驚愕。仕様=英語ヴォーカル・実尺300s+・シームレスループ・「Ka-ra-a-ge!」フック**なし**(ボス曲専用)。歌詞=`tools/lyrics_t48_dun.md`。`duration_sec=340`指定→**実尺332.5〜338.5s**(全30曲・実測比≈0.98=T37/T47の比率より高精度)。QA=実尺300〜400s/peak<-0.01dBFS(-0.7〜-2.9dBFS)/Vorbis 48kHz stereo/シームRMS比0.15〜6.0(04_a・06_aが初回アウト→retryで合格)。blind retry(12試行/曲・3ラウンド・argvフィルタ再実行)でVRAM競合(502/HIP OOM/connection refused)を耐え、00_a・00_bがラウンド1全失敗→ラウンド2で回収=**30/30 OK**。ファイル名不変=**ゲームコードBGM側ゼロ変更**(`themeBgmName`/`DUNGEON.bgm`は従来どおり3バリアント乱数選択)。

**② 回転タッチズレ根本修正（T47残存の根因特定）:** T47の`canvasPos()`アスペクトガード(contain正規化)は**誤り**だった=バッファは常にそのCSS rectへ**ストレッチ**され(レターボックスは`#wrap`flex=canvasの外)なので、rectとバッファ比の食い違いは「描画がcontainで縮小された」のではなく「**rectが遷移中の旧window値のまま**」だった。T47の`fitCanvas`early return(`return`でCSS未更新)が**CSSを旧window状態に固定**=「見えている位置より下をタップしないと反応しない」が固定された**本当の根因**。修正2点:
1. **`canvasPos()`=rect最新+単純ストレッチ1パス**に書き換え: `x=(cx-r.left)/r.width*W`(毎イベントで`getBoundingClientRect`取得)。バッファが常にrect全体へストレッチされる以上、これは常に正解。
2. **`fitCanvas()`の単調拡大保証(600ms)はバッファWの保持のみ**。CSS width/heightは**必ず**現在windowで再計算(early return廃止)。→遷移中の旧window値が来てもCSSは「今見えている」rectと一致し、`canvasPos`の単純ストレッチが常に正しい。`touchTol()`(+20%・300ms)は従来どおり維持。

**③ 町ショップ2行目レイアウト修正:** 雑貨屋(5施設目)追加で2行目が旧4施設向けレイアウトのまま→隣接重複+右端が画面外へ溢出。修正: 行2中心fx=**0.1/0.3/0.5/0.7/0.9**(W=960で96/288/480/672/864=等間隔192px・左右8px余白・パネル幅196→**176**(y>250の行2のみ)+行2名前14pxで収差解消)。行1(130/340/550/760)は不変。`townShopAt`(半径52)は自動追従のため変更不要。

**④ 上り階段（方案A・ユーザー確定）:** 「階段は下に降りた場所に上に行く階段を設置する。街に直通ではなく一階上に戻るだけ」を**入口マス方式**で実装:
- **配置**: 全階で`MAP.upstairs=MAP.entry`(**降りてきた場所=spawnマスそのもの**)。`genMap`両経路(主生成+フォールバック)に設定。描画=`stairs.png`上下反転+緑の上向き矢印(脈動)。入口マスは従来から配置reservedのためハザード/敵/宝箱/トラップ不被覆。
- **トリガー=待機のみ**: **踏むだけでは無反応**(=待機扱い・降下→即復帰ループ防止)。`endPlayerTurn(consumed,isWait)`に第2引数`isWait`追加し、**Space/タッチ待機ボタン**(`endPlayerTurn(true,true)`)でそのマスにいた場合のみ`goUpstairs()`発動=**攻撃/スキル/アイテムでは上らない**(意図しない離脱防止)。
- **遷移**: `goUpstairs()`=N≥2は`enterFloor(N-1,false)`で**一階上へ**(街には**直通しない**・新階のspawn=その階の上り階段に出現)、N=1のみ**地上(町)**へ(`setupTown`+bgm_town+「地上へ戻った。」トースト)。装備・金・Lv・`floorsVisited`保持。
- **中ボス封鎖の影響なし**(撤退OK・100Fボス階も99Fへ撤退可)。出口階段(降下)の中ボス封鎖は従来どおり維持。
- UI: enterFloorトースト2F+で「待機=上へ」ヒント+ヘルプに上り階段の文言追加。
- 旧実装(入口部屋の遠隅に配置+踏み判定で即町帰還)は**全面撤去**(`pickUpstairs`/`reserved`追加条件/`checkStairsDescend`の踏み判定を削除)。

**検証:** t48_test **36/36→T49で56/56**(A: 回転=縦長390×844起動→横回転でW=1169/600ms窓内で旧値でもW縮小なし+CSSは現在window追従(staleなし)/遷移中の旧window値でも「見えている位置」タップが正しい論理座標へ(中央=(wL/2,270)・75%高→y=405で圧縮なし)/touchTol維持 / B: 町2行目=96/288/480/672/864@960・等間隔192>パネル176・行1不変・W=1201で収まり・townShopAt新座標ヒット+旧座標不命中 / C: upstairs=entry全階(1/10/50/90/100)+FLOOR維持+seed再現/spawn直後は自動で上らない+checkStairsDescendでも無反応/待機のみ発動=20F→19F(街直通なし)+金・装備・Lv保持+visited記録/待機でないターン消費では上らない/1F→町/中ボス生存時でも上り階段撤退可+出口階段封じ維持/tryMovePlayerで上り階段マスへ=通常移動のみ/100F→99F/ハザード不被覆/待機×5で5→4→3→2→1→town(一階ずつ・ループなし)) + **全回帰16スイートgreen**(TOTAL **237** pass/0 fail=ベースライン201+t48 36)+`node --check`合格+audio/30本ffprobe全QA合格。

### T49 降り階段の整合性+選択制降下（2026-09-17）
**ユーザー指示:** 「2階に登る階段を登ると今は1階に登る階段があるがこれはおかしい。登った先には降りる階段があるはず。また降りるかどうかは選択できるようにして」「各階で同様の整合性必ず持たせて」。**実装中の指示変更:** 「下の階段を踏むと選択肢出現で、先へ進む。留まるを選べる」=降り階段を踏むと即降下ではなく**「降りる/留まる」の選択ダイアログ**。

**① 上り階段の到着点=降り階段(exit)の隣** — `goUpstairs()`→`enterFloor(f-1)`後に**`placeAtTopOfStairs()`**がexitの直交隣接床マスに到着:
- 優先順=**右→左→下→上**(固定=決定論・seed再現)。条件: `isFloor`のみ(穴/水/毒/柱不可)・`!enemyAt`(生きた敵不在)・ワープゲート/セーブポイント非所在・未開の宝箱非所在・トラップ非所在(着地即トリガー防止)。着地先にドロップがあれば`collectDrops`。全候補不可時はspawn(入口)のまま=フォールバック(exitは幅3以上の部屋の中心なので実質到達不能)。
- enterFloorのトーストを「NF → (N-1)F (上り階段) 降り階段がある」で上書き。

**② 踏むと「降りる/留まる」選択(自動降下廃止)** — 降り階段を踏むと`endPlayerTurn()`で**ターン消費だけ**(即降下しない)→敵フェーズ実行→末尾の`checkStairsDescend()`で**`openStairsChoice()`**(`UI.stairs={open:true,sel:0,from:floor}`)。
- 表示中は`updateTurn`が停止(=待機扱い・`G.paused||UI.bag||UI.gate||UI.stairs||UI.help`でearly return)。
- 操作: ←→/↑↓(A/D/W/S)=**エッジ検出**(`G._scL/_scR`)で2択切替(長押しの往復振動なし) / **Z/Enter=`stairsChoose()`**(sel0=`nextFloor()`で降下・sel1=「留まる」トースト+階段上に留まる) / **X/Esc=`closeOverlay()`=留まる**。
- 「留まる」=階段上に留まったまま(移動/待機が自由)。exit上で待機しても**自動降下は起きず再び選択が出る**(バウンスなし)。joystick/touch待機/Tabバグ/タッチバッグボタンも`UI.stairs`表示中はガードで無効化。

**③ 従来分岐の維持** — 100F=`bossDead`で`doClear()`(選択を挟まない)/未撃破は無反応(選択なし・降下なし)。10F帯中ボス生存時は**封鎖トーストのみ**(選択なし・撃破後に選択)。上り階段(入口)のT48動作=不変(踏むだけ無反応・待機のみ一階上・1Fのみ町)。

**④ フレームwatchdog(ゲーム側)** — `loop()`にtry/catch追加(update/draw/drawTouchUIが例外で飛ぶと**rAF鎖が静かに死滅し全操作が永久停止**する症状を捕捉+`[frame-watchdog]`ログ+復帰)。T49の検証中に`doClear`/`drawHUD`が**無効装備ID(テスト由来`w_iron`)**でクラッシュし得ることが発覚→`doClear`と`drawHUD`に存在チェックガード追加(破損セーブ耐性)。

**⑤ ハーネスバグ(根本原因・ゲーム側のバグではなかった)** — t48_testのサンドボックスがrAFを**単一スロット**で実装していたため、`fitCanvasNow()`(resize/orientationchange)が`requestAnimationFrame(anon→fitCanvas)`を登録すると**`loop`を上書きしゲームループが静かに死んでいた**。D2cで`TURN.t`が0.160で凍結・全早期returnフラグfalse・例外なし=この症状。修正: rAFを**キュー式**に(実ブラウザと同一の多重登録セマンティクス・1フレーム=その時点のキューの全コールバック実行)。rAF登録ログで`[loop, (anon), (anon), fitCanvas, (anon), fitCanvas]`が死因を特定。
- 附帯: `run_all_tests.sh`の集計が**"N passed / M failed"形式のみ**を数えており、"N pass / M fail"形式の4スイート(t41_t42/t41_gatekeys/t35_perf/t45_perf)がTOTALから脱落→**t41_t42のE11失敗(=T49で従来「討伐後即降下」期待が破れる=仕様変更)が237/0に隠れていた**教訓。両形式を数えるよう修正。E11をT49仕様に更新(E11a=討伐後に選択が出る/自動降下しない・E11b=「降りる」で11F)。

**検証:** t48_test **56/56**(既存A/B/C 36 + **Dセクション20**: D1 多階整合2/10/20/50/90/99F=上り階段の先が必ずその階の降り階段の直交隣接床マス / D2a 到着点=exit隣接かつexit上・入口でもない / D2b 踏んだ直後は即降下しない+exit上 / D2c 敵フェーズ後に選択ダイアログ出現 / D3a 「降りる」→21F(20→21往復) / D3b 21Fは入口spawn(T48ルール維持) / D4a 選択表示中=ターン停止(phase=player) / D4b 「留まる」=20Fのままexit上に留まる / D4c 留まった後=離脱自由 / D4d exit上で待機しても自動降下しない(再提示) / D5 中ボス生存=封鎖トーストのみ(選択なし) / D5b 撃破後=選択が出る / D6a 100F未撃破=無反応 / D6b 撃破後=clear(選択を挟まない) / D7a〜d 初期sel=「降りる」/右矢印で「留まる」/左矢印で復帰/X・Esc=留まる / D8a 入口で待機判定=何もしない / D8b 入口待機で11F(T48動作不変)) + **全回帰16スイートgreen**(TOTAL **338** pass/0 fail=257+t41_t42 54+t41_gatekeys 8+t35_perf 3+t45_perf 16・集計バグ修正で全数カウント)+`node --check`合格。

### T50 斜め攻撃+ぶつかった音+スティック方向転換化（2026-09-17・ユーザー指示3件）
**ユーザー指示:** ①「攻撃は斜め方向に可能。ただし、壁を挟む場合は攻撃不可とする。」②「左上のスティックは移動用ではなく、方向転換用とすること」→補正「間違い。方向転換はターン経過無しで。」（=ダンジョン内スティックは**ターン消費なしの向き変更のみ**）③壁挟みで拒否された攻撃の挙動=「ぶつかった音を出して向き変更だけ。」A/B判定=**A(直線壁への攻撃のターン消費撤廃)=却下**(従来動作維持)/**B(移動で壁にぶつかったら音を鳴らす)=合意**。

**① 斜め攻撃の壁挟みゲート** — `attackAction()`の**先頭**(習熟インクリメント/中ボス台詞/溜めより前)にゲート追加: 向きのベクトルが斜め(`dv[0]!==0&&dv[1]!==0`)かつ`!(pWalk(tx+dx,ty)&&pWalk(tx,ty+dy))`なら**攻撃不可**→`playBumpSFX()`+即return。判定=**斜め移動と同一ルール**(直交マスが片方でも歩けない=コーナーカット不可=壁挟み)。結果=**ターン消費なし・攻撃アニメ/SFX/toastなし・位置不変・溜め状態`charging`は維持**(溜め中の拒否で溜めが切れない)。全武器種・溜めの開始/発射に一律適用(分岐前にあるため)。

**② ぶつかった音`playBumpSFX()`** — 新SFX `audio/sfx_bump.ogg`(0.140s/6,671B・OGG Vorbis 44100Hz stereo・ソフトな低域ドスン~358Hz中心・50msフェードアウト・decode検証済み)。AUD_LIST登録(SFX 34→**35**・計78→**79**本)。`G.time`基準の**0.25秒スロットル**(`BUMP_LAST`・スロットル中の再試行は`BUMP_LAST`を更新しない=0.3秒後に鳴る)。発動箇所4つ: 斜め攻撃の壁挟みゲート / `tryMovePlayer`のブロック分岐3つ(直線方向の壁・直線方向の敵・斜め全面ブロック=逸らし不能)。**直線方向の壁への攻撃は音なし**(A却下=仕様変更なし・ターン消費の従来動作維持)。通常移動は足音のみ(不変)。

**③ 左上スティック=方向転換専用化(ダンジョン)** — `joyMove()`: ダンジョン中(`DUNGEON`+`TURN.phase==="player"`+`!UI.bag&&!UI.gate&&!UI.stairs`)はデッドゾーン(13px)超過で**リアルタイムに`DUNGEON.player.dirIdx=dirIndex(sign(dx),sign(dy))`のみ設定**(8方向・ターン消費なし・移動なし)。`joyRelease()`: ダンジョンでは旧の`tryMovePlayer`呼び出しを**削除**(離す瞬間の1マス移動を廃止)しJOY状態リセット+VKクリアのみ。町/メニューはVK方式(`joySet`)で**不変**。`drawTouchUI`にダンジョン時のみスティック上部へ「転」ラベル表示。`drawHelp`にタッチ操作行を追加(パネル660×508・行17px間隔25行に圧縮)。

**検証:** t50_test **48/48**(A: 前提・壁挟み(両方ブロック)=ターン消費なし+bump1回+アニメ/SFXなし / 開けてる斜め=命中+ターン消費+bumpなし / 片側開け(コーナー壁挟み)=攻撃不可+音 / 直線壁=従来どおりターン消費 / 溜め中の壁挟み=溜め維持+音 / B: 壁移動=音+向きのみ / 0.25sスロットル / 0.3s後に再鳴動 / 敵移動=音 / 通常移動=足音のみ / C: 8方向dirIdx即時設定(8/8)/ターン・移動なし / デッドゾーン無反応 / joyRelease=移動・ターンなし+JOY/VKリセット / 非playerフェーズ無効 / 階段選択中無効 / 町ではVK維持 / D: T49回帰=PAD8移動+上り階段20→19F+降り階段を踏む→選択→「降りる」で21F) + **全回帰17スイートgreen**(TOTAL **386** pass/0 fail=338+t50 48)+`node --check`合格。

### T51 移動時のスプライト「一瞬縮む」修正（2026-09-17・ユーザー報告）
**ユーザー報告:** 「勇者、聖騎士、無職の移動時の挙動がおかしい。なんかいったん小さくなる。確認して修正して。」

**原因:** 歩行フレーム(`*_walk.png`)の**内容bboxが静止フレームより小さい**(PIL実測: 勇者=静止(0,0,128,128)フル/歩行(28,37,100,91)・聖騎士=静止(18,17,78,66)/歩行(24,17,60,70)・無職=静止(28,18,68,78)/歩行(18,15,54,71))のに、従来は両フレームとも**キャンバス全体→34px**に拡大していたため、歩行中だけスプライトが~0.5〜0.8倍に縮んで見えた(移動の1フレーム目=歩行フレームで毎フレーム縮む)。

**実装:** (1)**`measureImgBBox(im)`** — `im.onload`時にアルファ>8のbboxを計測(try/catchで計測不能時は安全にスキップ)→`im._cb={x,y,w,h}`。全スプライト(66種×2)に自動適用。フォールバック=`_cb`なし時は旧挙動(キャンバス全体)で描画。(2)**`drawPlayer`の正規化** — 静止フレームの内容の見かけサイズ`dw=rc.w*s/rW, dh=rc.h*s/rH`(s=34・rW/rH=静止キャンバスのnaturalWidth/Height)を基準に、歩行フレームは**source rect=そのフレームの`_cb`**を`dw×dh`のdestに描画=静止と歩行で**内容サイズが完全に一致**。被弾フラッシュ(`tintOf`→`_white/_dark`系)も同一source rectで描画。敵/召喚獣/中ボス/ボスは報告スコープ外のため**不変**(T51の検証で実アセット5職のbbox実測値と整合を確認済み)。

**検証:** t50_test E1〜E5(E1 合成画像でbbox計測(10,20,80,60)正しく取得 / E2 実アセットの勇者・聖騎士・無職+賢者・召喚士のbbox実測値と一致 / E3a 静止フレーム=source(0,0,128,128)→dest 34×34(旧挙動と同一) / E3b 歩行フレーム=source(28,37,100,91)→dest 34×34(旧26.6px→34px=縮小解消) / E4 フラッシュも同一source rect / E5 `_cb`なし=フォールバックでキャンバス全体34×34) + 全回帰386/0 green+`node --check`合格。

**→ 2026-09-17 ユーザーから「治っていない(勇者/聖騎士/無職)。召喚士と賢者は問題ない」と再報告 → T52で静的テーブル化に改訂(下記)。**

### T52 T51の再発根絶 — 実行時bbox計測の廃止と静的テーブル化（2026-09-17・ユーザー再報告）
**ユーザー報告:** 「勇者、聖騎士、無職の移動時に画像が一瞬小さくなる件、治っていない。召喚士と賢者は問題ない。」

**診断（根本原因）:** T51の`measureImgBBox`(canvas `getImageData`)はユーザー環境(canvasのセキュリティポリシー等)で**`catch(e){}`によりサイレントに失敗**すると`im._cb`が未設定のまま**旧挙動(キャンバス全体→34pxストレッチ)へフォールバック**=「治っていない」の正体。PILで実アセットを再実測(α>8)しフォールバックの数式に代入すると報告と**1:1一致**: 勇者W-23%/H-31%・聖騎士W-25%・無職W-24%で縮む一方、召喚士=静止/歩行のbboxが完全一致なので0%(「問題ない」)、賢者=H-16%のみで目立たない(「問題ない」)。=ユーザーはフォールバック状態にいると確定。

**実装:** (1)`measureImgBBox`関数+`im.onload`内での呼び出しを**削除**=サイレント失敗の罠自体を撤去(ロード時79枚分のピクセルスキャンも不要化)。(2)**静的テーブル`SPRITE_BB`** — 5職×静止/歩行=10件をPILオフライン実測(α>8)でコードに埋め込み: `hero.png:[0,0,128,128]` / `hero_walk.png:[30,39,98,88]` / `paladin.png:[20,19,75,62]` / `paladin_walk.png:[26,19,56,66]` / `mage.png:[31,22,65,74]` / `mage_walk.png:[31,17,65,62]` / `summoner.png:[32,24,64,72]` / `summoner_walk.png:[32,24,64,72]` / `unjob.png:[30,20,66,76]` / `unjob_walk.png:[20,17,50,67]`。(3)`drawPlayer`は**source rect=`SPRITE_BB[現フレーム]`・dest=静止内容の見かけサイズ`rc[2]*s/rW × rc[3]*s/rH`**(数式はT51のまま)=**http/file/mobileを問わず静止/歩行の表示サイズが完全一致**。テーブル未登録ジョブは従来どおりキャンバス全体→34×34へフォールバック。被弾フラッシュも同一source rect。敵/召喚獣/中ボス/ボスは不変。

**検証:** t50_test E1a〜E5(E1a `measureImgBBox`廃止確認 / E1b `SPRITE_BB`10件定義 / E2 全5職の歩行内容見かけサイズ=静止時・報告3職は旧比+10%以上で解消 / E3a 静止フレーム=source `SPRITE_BB[hero.png]`→静止内容サイズ / E3b 歩行フレーム=source `SPRITE_BB[hero_walk.png]`→静止時と同じサイズ(旧縮小比を解消) / E4 フラッシュも同一source rect / E5 テーブル未登録ジョブ=全体34×34フォールバック) + **全回帰17スイート391/0 green**+`node --check`合格。

### T53 上下移動で体の向き(左右)がリセットされない（2026-09-17・ユーザー報告）
**ユーザー報告:** 「聖騎士とか移動すると左右に向く。おかしい」

**原因:** スプライトは左右反転1枚+上書きで8方向を表現する横スクロール系のため、反転が`DIRS[dirIdx][0]<0`で決まっていた。上下移動で`dirIdx`が2(下)/6(上)に変わると水平成分が0=**必ず右向きにリセット**(左向きで上へ移動すると右に反転する)。

**実装:** **体の向き(`faceX`)と狙い方向(`dirIdx`)の分離** — `makePlayer`に`faceX:1`追加(初期`dirIdx:0`=右向きと整合)。`drawPlayer`で`DIRS[P.dirIdx][0]!==0`(水平成分あり)のときのみ`P.faceX`を更新し、反転は`flip=(P.faceX||1)<0?-1:1`。**狙い方向(`dirIdx`)は一切不変**=攻撃・スキル狙い・向きハイライト・矢印・8方向攻撃・T50の斜め攻撃ゲート・召喚獣のdirIdx継承はそのまま(上を向いても上を攻撃できる)。見た目=上下移動中は「最後に移動した左右方向の体向きを維持」(横スクロール系JRPGの標準挙動)。

**検証:** t50_test F1〜F4(F1 `makePlayer`でfaceX=1 / F2 上下移動(dirIdx=6/2)でfaceX不更新+スプライト左維持(scale=-1) / F3 水平移動でfaceX更新+スプライト反転 / F4 実移動で上へ: dirIdx(狙い)=6かつfaceX(体)=左維持・位置は上へ1マス) + **全回帰391/0 green**+`node --check`合格。

### T54 ワープゲート=「解放済みゲート階↔地上」の自由往来（2026-09-17・ユーザー指示）
**ユーザー指示:** 「ワープゲートは各10階ごとにボスが守っている物との移動のみとする。一度解放することで自由に利用できることとする。」（追記Q&A: 移動先=**解放済みゲート階のみ(推奨)**・解放済みゲート階と地上は自由に行き来・ボス討伐後以降）

**実装:** (1)**新ヘルパー`gateFloors()`** — `SAVE.miniBossDefeated`(死亡/リロード後も永続)の10F帯を昇順返す。(2)`openGateMenu`の移動先リストを**「訪れた全階」(`SAVE.floorsVisited`)から解放済みゲート階のみに**=町のゲートもダンジョンゲートも同一リスト。空リストは「解放されたワープゲートはまだありません(10F帯の中ボスを討伐して解放)」表示。(3)**ゲートマスへのZ**=その階の中ボス**未討伐**なら**メニューを開かず**「○○ がワープゲートを塞いでいる…! 先に倒して。」トースト+`playBumpSFX()`のみ(`_gateDone`は設定しない=討伐直後に即再入OK)・**討伐済み**なら「地上(町)へ戻る」ボタン付きメニュー(`_gateDone`=1階入場1回のルールは不変)。(4)`mbDefeated`の**「地上ゲート」再配置を廃止**=ゲートは元のタイルに固定(`DUNGEON.miniGate`/`isMiniGate`削除)・討伐時`_gateDone=false`+トースト「階段とワープゲートが解放された。」(5)**100Fはゲートなし**(`genMap`/`genAttempt`を`floor<100`限定=中ボス不在で解放不能なため・100Fのセーブポイントは維持)。(6)文言更新: 町のゲートdesc「解放された階と地上を行き来」/メニュー副題「解放された階を選択」。

**検証:** t50_test **G1〜G7(17チェック)**(G1 `gateFloors()`=討伐済み10F帯のみ昇順 / G2 100Fゲートなし+セーブポイント維持・90Fはゲート+セーブポイント維持 / G3 未討伐ゲートにZ=メニュー出ず+「塞いでいる」トースト+ぶつかった音+`_gateDone`未設定 / G4 討伐後=メニュー+地上ボタン+リスト=[10]のみ / G5 地上選択→町へ / G6 町のゲート=解放階のみ[10,20]+地上ボタンなし・20F選択→ワープ・解放なし=空リスト / G7 ソースに`miniGate`代入・`isMiniGate`判定が残っていない) + **t41_t42_test(E7=討伐でゲートが移動しない/G=解放済み階リスト化)とt41_gatekeys_test(J=解放済み階でのメニューキー操作)の陳腐化期待をT54仕様に更新** + **全回帰17スイート408/0 green**+`node --check`合格。

### T55 階段ダイアログの階数ラベル=計算後に結合（2026-09-17・ユーザー報告）
**ユーザー報告:** 「階段表示が例えば5階に進む際に4+1と出る。計算がされずに文字列として表示されているのではないか?」+スクショ(1Fで2Fへ降下のダイアログ: サブタイトル「2Fへ下りる」は正しく、ボタンが「降りる (11F)」)。

**診断:** スクショの「降りる (11F)」は**現行コードのバグ出力そのもの**=`drawStairsChoice`のボタン`"降りる ("+s.from+1+"F)"`がJSの左から評価で文字列結合される(1F→「11F」/4F→「41F」/10F→「101F」)=ご報告の推測「計算がされずに文字列として表示」が的中。サブタイトル` s.from+1+"F へ下りる..."`は加算が先で正しい=**同一ダイアログで正誤が混在**。HUD「最深記録 1F」はプレイヤーが1Fなので正しい(誤検知解消)。併報の「スティックが上下左右に向かない」は現行コードでは再現経路なし(joyMove=sign-based 8方向・t50_test C2で8方向確認済み)+最新ビルド実行中の確定で**T50後の旧キャッシュ混在**と判断=コード修正なし(ハードリフレッシュ後再確認待ち)。

**実装:** 階数ラベルを**ヘルパーに集約して`(from+1)`を先に計算** — `stairsDescendText(from)`=`"降りる ("+(from+1)+"F)"`・`stairsSubText(from)`=`(from+1)+"F へ下りる / この階に留まる"`(明示的括弧で統一)。`drawStairsChoice`はヘルパー参照に置換(テスト可能化)。

**検証:** t50_test **H1〜H7**(H1 1F→「降りる (2F)」/H2 4F→「降りる (5F)」/H3 10F→「降りる (11F)」/H4 99F→「降りる (100F)」/H5・H6 サブタイトル同型/H7 ソースに旧結合式なし) + **全回帰17スイート415/0 green**+`node --check`合格。

## 最終成果物
- **ゲーム本体**: `index.html` 単一ファイル（AUD_LIST=**79本**: BGM **44** + SFX **35**・ボイス0。**T47: SFXはWeb Audio再生で`<audio>`要素不使用**=起動Audio生成44本のみ。**T50: 新SFX `sfx_bump.ogg`追加**）
- **audio/**: **79本** = 旧69 + 中ボス専用BGM 9本(`bgm_mb_01〜09`) + **ぶつかった音1本(`sfx_bump.ogg`・T50)**。AUD_LISTと完全一致・余剰なし。**T47: 全10ボス曲(`bgm_mb_01〜09`+`bgm_boss`)が`Ka-ra-a-ge`表記・約2分+シームレスループで再生成完了・QA 10/10合格**。**T48: ダンジョンBGM全30曲(`bgm_dun_00〜09_a/b/c`)が実尺332.5〜338.5s(5分+・英語ヴォーカル・シームレスループ・10F帯エモーションアーク)で再生成完了・QA 30/30合格**
- **SFX20本（新規）**: 「生成窓下限2秒→無音トリム→ピーク-6dB正規化→QA」で全件完成。**T50: `sfx_bump.ogg`(0.140s・低域ドスン)追加**
- **テスト**: 全**17スイート** green（`/tmp/opencode/run_all_tests.sh`・TOTAL **415** pass/0 fail・t50_test=T50/T51b/T53/T54/T55の**77チェック**）+ `node --check` 合格

## 履歴（主要な出来事・教訓）
1. **短尺指定のSFX生成は壊れる**: 0.55s指定=クリップ方形波（ソースWAV破損）/ 1s指定=内容劣化 → **下限2秒が確定方針**（ユーザー指示）。
2. **日本語TTSボイス不自然** → ボイス全廃。AUD_LISTから25本削除、`playVoice()`はAUD未登録ならno-op（呼び出し側残置でロジック影響ゼロ）、テスト6アサーションを廃止仕様に更新。
3. **sonic.asset_idの罠**: `sonic.generate`応答のtop-level `asset_id`(`job-result:`形式)は`sonic.pack`で422。本物は`output.asset_id`(`asset:<uuid>`)。フォールバック=資産ストアWAV(`/data1tb/ControlDeck/data/feature-data/sonic-forge/assets/<uuid>.wav`)からffmpeg直接変換。
4. **サブエージェントのキャンセルでファイルが到達する**: 統合audioジョブキャンセル直前にボイス21本+SFX17本の上書きが到達→**キャンセル後も必ず最終sweep**（voice削除+SFXのmtime/ffprobe点検→`/tmp/opencode/sfx_final/`から再配置）。
5. **GPU VRAMは共有・常時占有される**: 他プロセス(llama-server等)がVRAMを保持するとACE-StepはLM初期化でOOM。対策=**VRAMゲート待ちではなく即時再挑戦のバックグラウンドループ**（失敗は~3.5minで低コスト）。VRAM%閾値待機は占有が解消されない限り永久ブロックされる。
6. 他バグ修正: `mbTalk`の警告フィルタ / `drawPlayer`の`globalThis.P` / t28_regress flake（fuzz前フルHP）— 全て回帰テスト済み。
7. **TTSの日本語発音は表記依存で不安定（T47）**: `Karaage!`=「カラージ」にglide。**probe 1曲でユーザーの耳に確認→勝った表記で全再生成**が最速の確実手順（全生成→失敗は10倍のロス）。モーラ区切り`Ka-ra-a-ge!`+硬い`!`（チルダ`~`はglideを強化するので廃止）で「唐揚げ」に。
8. **回転中のwindowサイズは旧値が混在する（T47→T48）**: `orientationchange`の1回だけ・150ms遅延では旧値で確定し得る。即実行+複数回の再実行(rAF×2+60ms)+単調拡大保証(600ms窓)+タッチ許容拡大(+20%・300ms)で吸収。**T47のcanvasPosアスペクトガード+fitCanvas early returnは誤修正だった**=バッファは常にCSS rectへストレッチされ(レターボックスはcanvas外の`#wrap`側)ので「rect≠バッファ比」は旧window値のCSS固定が原因。根本修正=**CSS width/heightは常に現在windowで再計算し、単調拡大保証はバッファWの保持に限定**+`canvasPos`=rect最新+単純ストレッチ1パス。教訓: **座標系が「常にストレッチ」なら変換は1パスの単純比例で足り、アスペクト照合は不要(むしろ誤診を誘う)**。
9. **BGMの指定秒→実尺比は安定して約0.98（T48）**: `duration_sec=340`→実尺332.5〜338.5s(全30曲・散らばり±3s)。「300s+」要件は**340指定が正解**(T37の70→60-68s・T47の125→110-135sより高精度)。実尺が常に短めになる前提で**要件+約10%の指定**が確実。
10. **VRAM競合はblind retryで耐えるのが確実（T48）**: llama-server等他プロセスのVRAM占有(3〜31GiB振動)で502/HIP OOM/connection refusedが乱発。失敗ごとに即時再挑戦のblind retry(12試行/曲)+ラウンド制で、失敗曲だけ次ラウンドに繰り越せば**30/30到達**(00_a・00_bはラウンド1全失敗→ラウンド2で成功)。ゲート待ち(一定VRAM確保まで停止)は占有が解消されずブロックされる(既知の教訓5と整合)。
11. **ヘッドレスハーネスのrAFは「単一スロット」にするとゲームループが静かに死ぬ（T49）**: 実ブラウザのrAFは**多重登録(キュー)**される。`fitCanvasNow`の`requestAnimationFrame(anon→fitCanvas)`が`loop`の上書き登録を許すと、次フレーム以降loopが一切呼ばれず**TURN.tが一定値で凍結・全早期returnフラグfalse・例外ゼロ**=「動かなくなった」が症状のみで原因が一切見えない。診断=**rAF登録ログ(cb名)の末尾が`loop`か**で即判別。教訓: ハーネスは必ずキュー式rAF(1フレーム=その時点のキューの全cb実行)。附帯教訓: **テストランナーの集計パターンの見落としが失敗を隠す**(旧runnerは"N passed / M failed"形式のみ数え、"N pass / M fail"形式4スイートがTOTALから脱落→仕様変更による失敗が「237/0 green」に紛れ込んでいた)。
12. **フレーム例外はrAF鎖を静かに殺す（T49）**: `loop()`内の例外はrAFの再登録を飛ばし、以降のフレームが一切走らない(=全操作永久停止)。対策=`loop()`にtry/catch+`[frame-watchdog]`ログで復帰。検証中、C5cのテスト用無効装備ID(`w_iron`)が`doClear`/`drawHUD`の`ALL_ITEMS[id].x`参照でクラッシュし得ることが発覚→存在チェックガード追加(破損セーブ耐性)。
13. **SFXの再生検証には`unlockAudio()`が必須（T50）**: SFXはWeb Audio方式で、`SFX_BUF`にdecode済みのbufferが入って初めて`playSFX`が再生する(未登録=サイレントno-op)。`SFX_BUF`は初回ユーザー操作(`unlockAudio()`→`sfxWarmup()`)で埋まるため、**テストでSFXの再生を検証する場合は必ず先に`R("unlockAudio();")`**を呼ぶ(ハーネスのthenableが同期解決するためdecodeも即時完了)。呼ぶと`_sfx`にURLが溜まる=カウント可能。
14. **スロットル/時間依存のアサーションは`G.time`を手動で進める（T50）**: `playBumpSFX`の0.25sスロットルは`G.time`基準。テストでpumpしない区画では`G.time`が進まないので、音を期待する攻撃の前に`R("G.time+=0.5;")`でスロットルを解放する。また`TURN.phase`は攻撃の直後"anim"になるため、**ターン消費の有無は`DUNGEON.turn`の増減で判定**する(phaseの残留値で判定しない)。
15. **歩行フレームと静止フレームの内容サイズは揃っていない（T51→T52）**: 差分生成の歩行フレームはアセットごとに内容bboxが静止側と異なる(勇者=フルキャンバスvs98×88等)。両方を「キャンバス全体→固定px」で描画すると歩行中だけ縮んで見える。**描画は各フレームの内容bboxをsource rectにして、静止フレーム内容の見かけサイズに正規化**するのが正しい。ただしbboxの**実行時計測(`getImageData`)は環境によりサイレントに失敗し、`catch(e){}`がそれを隠すと旧挙動へ無言フォールバック**(T51の「治っていない」=これ)。**ユーザー環境の挙動に依存させないなら、PIL等オフライン実測値を静的テーブル(`SPRITE_BB`)に埋め込むのが確定**。
16. **「治っていない」報告はまずフォールバック経路の再現で検証する（T52）**: 新実装が「一部環境で無言に旧挙動へ戻る」構造を持っていると、報告の症状分布(勇者/聖騎士/無職だけ縮む・召喚士・賢者は問題ない)がフォールバック側の数値と**1:1一致**して根本原因が確定した。症状が「一部のジョブ/一部の端末だけ」=実装が分岐(計測成功/失敗)に依存していないかを先に疑う。
17. **スプライトの「体の向き」と「狙い方向」は別物（T53）**: 8方向の狙い(`dirIdx`)を左右反転に直接使うと、上下移動のたびに体が右にリセットされる(横スクロール系スプライトで不自然)。**反転は`faceX`(最後に水平移動した方向)で決める**のが正解で、狙い方向(`dirIdx`)は攻撃・ハイライト・矢印・斜め攻撃ゲート用にそのまま残す。分離は`faceX:1`1フィールド+更新条件1行+反転式1行で済む。
18. **`sign()`は非ゼロなら何でも±1になる（T56）**: 8方向スティック入力で**径方向デッドゾーンのみでは不十分**。実機タッチには常に1〜12pxの交差軸ノイズがあるため、上下左右入力が常に斜めになる。**軸別デッドゾーン**(主軸>dzなら交差軸<dzをゼロ化)が正しい。テストが通るのは入力の片方が**ちょうど0**だから=ハーネスは実機のノイズを再現していない。ノイズ入力を食わせるテスト(K1〜K4)が確定手順。
19. **TTSの漢字読みは表記依存で暴く（T57）**: 「唐揚げ」が**「タラアゲ」**と読まれる事故(「唐」=タラ解釈)。TTSテキストに**ひらがな「からあげ」**で指定すると解決(T47の`Ka-ra-a-ge`表記修正と同一種=日本語TTSの発音は常にASR書き起こしでQAすること)。
20. **テストセクションは前セクションが閉じなかったUIオーバーレイをクリアする（T56）**: Gセクション(ゲートテスト)が`UI.gate`を開いたまま残しKセクションに持ち越すと、`joyMove`はUI開閉中に無効(`!UI.gate`)で**無言にno-op**→4チェックが失敗。セクション先頭で前セクションが開いたUI状態(gate/bag/stairs)を必ず`null`クリアする。

## 重要パス
- ゲーム本体: `index.html`（AUD_LIST@87）/ 仕様: `PLAN.md` / 説明: `README.md`
- テスト: `/tmp/opencode/*.js` + `/tmp/opencode/run_all_tests.sh`（**17スイート**・t48_testにT49のDセクション(20チェック)追加済み・t50_test=T49〜T58系列の**101チェック**(A斜め攻撃/Bぶつかった音/Cスティック/D T49回帰/E T51b静的bboxテーブル/F T53 faceX分離/G T54ゲート/H T55階段ラベル/I T57死亡=全回復+おばあさん声/J T58死に戻りBGM/K T56軸別デッドゾーン)・集計は両形式対応済み・**439/0 green**)
- T45計測: `/tmp/opencode/t45_measure_before.js`（before再構築+計測）/ `t45_measure.js`（after実測）
- T43音源: 歌詞 `tools/lyrics_t43_mb.md` / QA `qa_mb_bgm.sh`（9/9合格）/ 検証済みコピー `/tmp/opencode/mb_bgm_final/`（9本）/ 再試行スクリプト `retry_mb_bgm_blind.py`（完了・ログ `mb_bgm_blind.log`）
- SFX再生成ツール: `/tmp/opencode/regen_sfx_all.py` / `sfx_recover.py` / 検証済みコピー `/tmp/opencode/sfx_final/`
- 旧audioバックアップ: `/tmp/opencode/karaage_bak/`（`voice_discarded/`に廃止ボイス25本分）
- BGM歌詞仕様: `tools/lyrics_t37_boss.md` / `tools/lyrics_t43_mb.md`（**T47: 全歌詞=`Ka-ra-a-ge`表記へ置換済み**）
- T57/T58音声資産: `audio/voice_grandma_die.ogg`(老婆`grandma`死亡セリフ・voice:c33e644f・TTSはひらがな「からあげ」指定) / `audio/bgm_res.ogg`「死に戻り」テーマ曲(女性ボーカル/日本語歌詞/123s) / 歌詞 `tools/lyrics_t58_res.md`
- T47 BGM再生成: `/tmp/opencode/karaage_regen_make_args.py`(10曲args・`karaage_regen_args/`) + `karaage_regen_run.py`(blind retry+QA+argvピンポイント再実行・ログ `karaage_regen.log`) + `karaage_regen_watchdog.py`(失敗曲自動再実行) / probe: `/tmp/opencode/karaage_probe_01.ogg`+`karaage_probe_args.json`
- T48 BGM再生成: `/tmp/opencode/dunbgm_regen_make_args.py`(30曲args・`dunbgm_regen_args/`) + `dunbgm_regen_run.py`(blind retry 12試行/曲+ラウンド制+QA+dur300-400s/seam 0.15-6.0・ログ `dunbgm_regen.log`) / 歌詞: `tools/lyrics_t48_dun.md`
- sonic-forge DB: `/data1tb/ControlDeck/data/feature-data/sonic-forge/sonicforge.db`
- MCP直接呼び出し: `http://127.0.0.1:8765/api/v1/addons/agent-mcp/{tools,call}`（トークン=opencode.jsonの`CONTROL_DECK_ADDON_MCP_TOKEN`）

## 今後の拡張メモ（未着手・任意）
- ボイスを復活させる場合: AUD_LIST登録+ファイル配置のみで復帰可能（`playVoice`配線は残存。T40/T41の台詞文は`MBV_LINES`/必殺技定義に保存済み）
- T45の残課題（任意）: 敵/パーティクルのスプライトバッチ描画（本計測では敵48体でも<8ms/フレームのため未実施・リスク対効果で保留）
- git: master・未コミット。`audio/`はuntracked（バックアップは`/tmp/opencode/karaage_bak/`）
